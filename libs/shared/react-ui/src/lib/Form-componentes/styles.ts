import { FaInfo } from 'react-icons/fa';
import styled from 'styled-components';

export const StyledInfoCircle = styled(FaInfo)`
  font-size: 18px;
  color: #757575;
  :hover {
    cursor: pointer;
  }
`;
